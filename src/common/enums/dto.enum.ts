export enum SortType {
  DESC = "desc",
  ASC = "asc",
}

export enum SortBy {
  RECENT_APES = "updatedAt",
  CREATION_TIME = "created_timestamp",
  MARKET_CAP = "market_cap",
  REPLIES = "chat_count",
}

export enum SortTypeNumber {
  DESC = -1,
  ASC = 1,
}
